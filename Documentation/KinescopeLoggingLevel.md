# KinescopeLoggingLevel

Interface for logging type

``` swift
public protocol KinescopeLoggingLevel 
```

## Requirements

### part(of:​)

Checks that element is part of array

``` swift
func part(of array: [KinescopeLoggingLevel]) -> Bool
```

#### Parameters

  - array: array of elements
